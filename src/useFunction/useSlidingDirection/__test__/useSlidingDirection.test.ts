import { describe, expect, test } from 'vitest'
import { useSlidingDirection } from "../index";

describe('useSlidingDirection', () => {
    test("useSlidingDirection", async () => {
        // 
    });
});